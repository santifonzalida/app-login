import React from 'react';
import { render, screen } from '@testing-library/react';
import { DefaultPage } from '../../components/DefaultPage';

describe('<DefaultPage />', () => {
    test('should show text: login demo app', () => {
        //render(<DefaultPage />);
        //const text = screen.getByText(/login demo app/i).textContent;
        //expect(text).toBe('This is a login demo app created using');
    })
});
